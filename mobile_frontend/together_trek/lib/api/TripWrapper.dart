import "package:together_trek/api/httpRequest.dart";

// will implement fully when endpoints are added
// int updateTrip(String tripId, String data) {
//   final response = httpPut("trips/${tripId}", data);
// }
